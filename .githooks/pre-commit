#!/usr/bin/env bash
# Blocks commits containing credentials. Enable once per clone with:
#   git config core.hooksPath .githooks
exec "$(git rev-parse --show-toplevel)/scripts/scan-secrets.sh" staged
